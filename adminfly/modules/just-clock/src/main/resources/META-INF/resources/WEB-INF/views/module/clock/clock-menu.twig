<div class="navbar navbar-expand-lg fixed-top navbar-dark bg-primary">
    <div class="container">

        {% include './../../_shared/layout/partials/menu/logo/logo.twig' %}

        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="nav navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" id="modules" aria-expanded="true">
                        <b>{{ 'label.theme' | translate }}</b> <span class="caret"></span>
                    </a>
                    <div class="dropdown-menu" aria-labelledby="modules">
                        {% for theme in extra.get('values.themes') %}
                            <a
                                    class="dropdown-item"
                                    href="{{ relative(theme.path) }}">&bullet;&nbsp;{{ theme.label }}</a>
                        {% endfor %}
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" id="languages"
                       aria-expanded="false">{{ 'label.languages' | translate }} <span class="caret"></span></a>
                    <div class="dropdown-menu" aria-labelledby="languages">
                        {% include './../../_shared/layout/partials/menu/languages/links.twig' %}
                    </div>
                </li>
            </ul>
        </div>
    </div>
</div>